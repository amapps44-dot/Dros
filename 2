<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>خطواتك الأولى نحو الإنجليزية</title>

<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&family=Sora:wght@600&family=Quicksand:wght@600&display=swap" rel="stylesheet">

<style>
:root{
  --bg-1:#4C1D95;
  --bg-2:#7C3AED;
  --bg-3:#DB2777;
  --chalk:#FFF7FB;
  --paper:#FFFDF9;
  --ink:#2D2A4A;
  --card-back:#4C1D95;
  --accent:#FFD23F;
  --ar:'Tajawal',sans-serif;
  --en:'Quicksand',sans-serif;
}

*{box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{
  margin:0;
  background:
    radial-gradient(ellipse at 15% 0%, var(--bg-2), transparent),
    radial-gradient(ellipse at 90% 70%, var(--bg-3), transparent),
    linear-gradient(135deg, var(--bg-1), var(--bg-2));
  color:var(--chalk);
  font-family:var(--ar);
  line-height:1.7;
}

/* HEADER */
.hero{
  padding:60px 20px 80px;
  text-align:center;
}
.hero h1{
  font-size:2.2rem;
  margin:10px 0;
}
.hero .en{
  font-family:var(--en);
  color:var(--accent);
}
.cta{
  display:inline-block;
  margin-top:20px;
  padding:12px 25px;
  background:var(--accent);
  color:#000;
  border-radius:8px;
  font-weight:700;
  text-decoration:none
